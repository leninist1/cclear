"""
CClear 测试大纲

项目：CClear - Windows 桌面端磁盘管理工具原型
分支：test
编写日期：2026-04-08

================================================================================
第一原则（最高优先级）：
  在测试软件时，绝不使用软件的任何会导致删除文件的功能或操作。
  如果要执行这类操作，必须自己创建新的文件夹/文件，在新建的文件夹/文件上执行
  带有删除的功能或操作。所有涉及"移入回收站"、"清空回收站"、"清理临时文件"
  的测试必须在自建的临时测试目录/文件上执行，绝不触碰真实的系统文件或用户文件。
================================================================================

一、测试范围
  1. services.py - 核心服务层（扫描、搜索、清理分析、回收站操作、缓存）
  2. cache_store.py - 缓存存储层（快照读写、校验、删除）
  3. app.py - GUI 应用层（界面逻辑、事件队列、后台任务）

二、测试类别

A. 单元测试（Unit Tests）
  A1.  路径处理工具函数
       - normalize_path：绝对路径规范化、大小写统一、尾部分隔符去除
       - is_path_inside：子路径判断、边界情况（盘符根、UNC路径、大小写）
       - is_protected_path：系统保护目录识别
       - build_risk_level：风险等级判定逻辑
       - extension_label：扩展名提取与无扩展名情况
       - get_path_depth：路径深度计算

  A2.  扫描功能（scan_directory 相关）
       - 空目录扫描
       - 单文件目录扫描
       - 嵌套目录扫描
       - 符号链接跳过
       - 无权限目录跳过
       - 大量文件扫描（性能）
       - 缓存快照读取与复用
       - 强制重扫绕过缓存
       - 缓存过期检测
       - 无效路径异常

  A3.  搜索功能（search_files 相关）
       - 关键词搜索
       - 最小大小过滤
       - 结果数量限制
       - 空关键词处理
       - 缓存优先搜索 vs 实时遍历搜索
       - 缓存过期后的回退行为
       - 大小写不敏感匹配

  A4.  清理分析（analyze_cleanup 相关）
       - 用户临时目录分析
       - Windows临时目录分析
       - 下载目录大文件建议
       - 年龄阈值过滤
       - 估算可回收空间
       - 目标路径不存在时的处理

  A5.  缓存存储（cache_store 相关）
       - 快照保存与加载
       - 版本校验
       - 路径校验（不匹配则返回None）
       - 快照删除（仅删除自建缓存）
       - 损坏JSON文件的处理
       - 自定义缓存目录（CCLEAR_DATA_DIR）

  A6.  回收站操作（仅在自建测试文件上执行）
       - 受保护路径拒绝（应抛出 ValueError）
       - 不存在路径拒绝（应抛出 FileNotFoundError）
       - 普通文件移至回收站（在自建临时文件上）
       - 普通目录移至回收站（在自建临时目录上）
       - 清空回收站操作

  A7.  临时清理（run_temp_cleanup）
       - 成功计数与失败计数
       - 回收空间统计
       - 部分失败情况
       - 自定义 mover 注入

  A8.  ProgressReporter 进度回调
       - 回调函数调用
       - 节流频率控制

B. 集成测试（Integration Tests）
  B1.  扫描→搜索→缓存联动
       - 扫描后缓存写入，搜索可复用缓存
       - 缓存失效后搜索回退到实时遍历
  B2.  清理分析→临时清理流程
  B3.  缓存写入→读取→删除→重新扫描

C. 边界与异常测试
  C1.  超长路径 (>260字符)
  C2.  包含特殊字符的文件名（中文、空格、unicode）
  C3.  空文件（0字节）扫描与搜索
  C4.  极大数量文件目录（性能测试）
  C5.  权限不足目录
  C6.  并发操作（多线程安全性）
  C7.  缓存文件损坏或版本不匹配

D. GUI 测试（app.py）
  D1.  格式化工具函数（format_bytes, format_count, risk_tag）
  D2.  UI 组件初始化验证
  D3.  事件队列机制

三、测试执行原则
  1. 第一原则：绝不删除非测试创建的文件。所有删除类操作在 tempdir 中执行。
  2. 每个测试用例 setUp 创建独立临时目录，tearDown 自动清理。
  3. 涉及回收站的功能使用自建临时文件/目录。
  4. 不测试真实系统目录（C:\Windows等）的删除行为。
  5. 不测试真实临时目录的清理行为（使用 mock 或自建目录）。
  6. 不测试真实回收站清空（仅验证调用逻辑，不实际执行）。

四、预期 Bug 探查方向
  1. 路径处理：大小写不一致、路径分隔符、UNC路径
  2. 竞态条件：扫描过程中文件系统变化
  3. 缓存一致性：缓存过期但仍在使用
  4. 内存泄漏：大量文件扫描时内存增长
  5. 异常处理：OS权限错误、文件占用
  6. 回收站 API 失败场景
  7. ProgressReporter 节流逻辑
"""
